# Authentication Configuration
# Copy this file to .env.local and update with your actual values
# All variables prefixed with VITE_ are exposed to the client-side code

# Username for authentication (required)
VITE_AUTH_USERNAME=admin

# SHA-256 hash of your password (required)
# Generate using: echo -n "your-password" | shasum -a 256
# Example: echo -n "mypassword" | shasum -a 256
VITE_AUTH_PASSWORD_HASH=your-hashed-password-here

# Session duration in hours (required, must be positive number)
# Default: 24 hours
VITE_SESSION_DURATION=24

# Example of generating a password hash:
# For password "mypassword123":
# echo -n "mypassword123" | shasum -a 256
# Result: 6ca13d52ca70c883e0f0bb101e425a89e8624de51db2d2392593af6a84118090